name: webview_auth_tizen
description: A new Flutter plugin project.
version: 0.0.1
homepage:

publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  ewk_webview_flutter_tizen:
    git:
      url: https://github.com/bwikbs/plugins.git
      ref: 50ac06ef0278cdf8ab3d814ab3aee7d0fabe6135
      path: packages/ewk_webview_flutter
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  plugin:
    platforms:
      tizen:
        pluginClass: WebviewAuthTizenPlugin
        fileName: webview_auth_tizen_plugin.h
